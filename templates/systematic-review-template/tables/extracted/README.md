# Extracted Tables

Guardar aquí las tablas extraídas desde PDFs en formatos reutilizables como CSV o Markdown.

- Registrar cada tabla en `../evidence-manifest.csv`.
- Indicar si la extracción se hizo por OCR de PDF, parsing tabular o lectura multimodal.
- Priorizar tablas comparativas o condensadas que aporten directamente a la argumentación del paper.
